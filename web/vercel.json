{
  "build": {
    "env": {
      "PYTHON_VERSION": "3.11"
    }
  },
  "buildCommand": "pip install -r requirements.txt && python hospital_web/manage.py migrate",
  "installCommand": "pip install -r requirements.txt",
  "outputDirectory": ".vercel/output"
}